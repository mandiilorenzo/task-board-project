import Head from "next/head";

export default function DashboardPage() {
    return (
        <main>
            <Head>
                <title>Meu painel de tarefas</title>
            </Head>

            <h1>Pagina painel</h1>

        </main>
    );
}